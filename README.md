# webpack-4-hot-reload

Template make with Webpack 4 which hot reloads html,css,js and node server files.

To install dependencies: 

    npm install

and to watch files project:

    npm run dev
    
or to build project:

    npm run build